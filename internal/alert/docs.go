/*
package alert

This package defines inner-workings of sending notifications.

The methods prefixed with `New` will return a channel instance that
implements the `Channel` interface and which can be used throughout
the other packages.
*/
package alert
